export interface StepToReturn {
    certificateStepId: string;
    comment?: string;
    confirmed: boolean;
    fileId?: any;
    id: string;
    startDate: Date;
    stopDate: any;
    userId: string;
}
